; # 脑血管多类别分割项目 (TopBrain 2025) 行为准则

; ## 1. 核心技术栈
; - [cite_start]深度学习框架: 必须优先使用 PyTorch 和 MONAI (Medical Open Network for AI) 
; - [cite_start]影像处理库: 使用 SimpleITK 处理 .nii.gz 文件 。
; - [cite_start]任务背景: 全脑血管 42 类精细标注，面临极度类别不平衡和拓扑断裂挑战 

; ## 2. 3D 影像规范
; - 张量维度: 始终遵守 (Batch, Channel, D, H, W) 顺序。
; - [cite_start]坐标系: 严格处理各向同性重采样（通常为 1.0mm³）并保持 Orientation 对齐 
; - [cite_start]空间先验: 在实现模型输入时，务必包含 CoordConv 逻辑（拼接 x, y, z 归一化坐标通道） 

; ## [cite_start]3. 损失函数逻辑 (关键) [cite: 115, 116]
; - 复合损失公式: $L_{total} = \eta_1 L_{Dice} + \eta_2 L_{CE} + \eta_3 L_{clDice} + \eta_4 L_{clCE} + \eta_5 L_{Topology}$。
; - [cite_start]拓扑约束: 实现 clDice 或 cICE 时，必须使用可微分的软骨架化算法 
; - [cite_start]类别加权: 针对小血管分支（如 AcomA, PcomA）,在计算 Cross Entropy 时应应用更高的权重 

; ## 4. 目录保护与编码习惯
; - 禁止修改: `data/` 目录下的任何原始 .nii.gz 文件。
; - 配置文件: 所有的超参数（learning_rate, lambda_weights, patch_size）必须从 `config.yaml` 读取。

; ## 5. 交互偏好
; - [cite_start]在修改模型架构（如 ERNet 的 REAM 模块）前，请先打印出预期的特征图尺寸变化 
; - [cite_start]报错调试时，优先检查 CUDA 显存占用和 3D Patch 裁剪逻辑 